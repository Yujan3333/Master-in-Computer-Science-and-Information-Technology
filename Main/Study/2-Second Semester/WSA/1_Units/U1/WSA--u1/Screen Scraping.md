Screen scraping means automatically copying information from a website’s pages — just like a person would read and copy it, but done by a computer program.

For example, a script might visit a weather website, read the temperature shown on the page, and save it somewhere else. It’s often used when a website doesn’t provide an API to get the data directly.