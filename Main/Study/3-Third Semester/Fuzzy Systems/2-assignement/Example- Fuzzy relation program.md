
## 1️⃣ Relation R (X → Y)

```
Enter number of elements in relation R: 3
Enter (x y value) for R:
a b 0.9
a c 0.7
b c 0.1
```

* **Explanation**: 3 elements in R. Each line has: `x y membership_value`.

---

## 2️⃣ Relation S (Y → Z)

```
Enter number of elements in relation S: 3
Enter (x y value) for S:
b d 0.5
c d 0.8
c e 0.3
```

* **Explanation**: 3 elements in S. Each line has: `y z membership_value`.

---

## 3️⃣ Fuzzy Set A (for Cylindrical Extension X)

```
Enter number of elements in fuzzy set A: 3
Enter (element value) for A:
a 0.6
b 0.9
c 0.8
```

* **Explanation**: Each line has: `element membership_value`.

---

## 4️⃣ Elements of Y for Cylindrical Extension X

```
Enter elements of Y: b c d e
```

* **Explanation**: Just a list of Y-elements space-separated.

---

## 5️⃣ Fuzzy Set B (for Cylindrical Extension Y)

```
Enter number of elements in fuzzy set B: 3
Enter (element value) for B:
d 0.7
e 0.4
f 0.9
```

* **Explanation**: Each line has: `element membership_value`.

---

## 6️⃣ Elements of X for Cylindrical Extension Y

```
Enter elements of X: a b c
```

* **Explanation**: Just a list of X-elements space-separated.

---

### ✅ Notes:

1. For **relations**, always input **x y value**.
2. For **fuzzy sets**, always input **element value** per line.
3. For **cylindrical extensions**, the X or Y lists are just **elements, no values needed**.

---
